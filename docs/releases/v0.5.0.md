# v0.5.0 - Operator MCP + Codex Application Layer

Status: published. Package metadata and `cosheaf.__version__` record `0.5.0`.
The public `v0.5.0` annotated tag, GitHub release, post-tag release smoke, and
downstream workspace/public-KB pin updates are complete.

This release packages the Operator MCP + Codex Application Layer work that
landed after the published `v0.4.0` Strategy Planner + Research Task Graph
release. It makes the existing CLI-first research workflow easier for
Codex-style external operators to drive through documented runbooks, optional
MCP tools, and controlled draft/review/runtime write surfaces while preserving
Cosheaf's existing validation, gate, human review, public/private, verifier,
provider, and promotion boundaries.

This release is not production-ready software. It does not add
automatic theorem proving, full Lean/mathlib/CSLib proof checking of informal
statements, automatic autoformalization, automatic informal/formal semantic
alignment, automatic accepted promotion, AI-as-human-review, a web UI, SaaS
behavior, multi-user permissions, default-on provider calls, or MCP authority
over accepted knowledge.

## Highlights

- Read-only operator MCP surface:
  the optional stdio MCP adapter exposes whitelisted workspace, validation,
  gate, PR-checklist, public memory, public-only context, public-scoped
  strategy, research-run read/report, and deterministic eval smoke tools.
- Controlled draft/review/runtime MCP writes:
  narrow controlled tools wrap existing service-layer logic for draft
  artifacts, staged source notes, WorkerBundle validation/staging, draft
  review requests, checked counterexample evidence staging, non-accepted
  failure-log appends, research-run provenance, and strategy review exports.
- Service-layer boundary:
  MCP tools call typed Python services or CLI-equivalent policy surfaces. They
  do not expose arbitrary shell, environment inspection, hosted provider calls,
  accepted writes, promotion, human-review creation, or verifier-result
  mutation.
- Operator runbooks and demos:
  framework docs describe a CLI-first operator flow, MCP configuration as an
  optional adapter, workspace demo steps, final verification expectations, and
  PR summary requirements.
- Public KB policy smoke:
  public KB docs and guard checks explicitly reject treating operator, MCP,
  provider, LLM, checked-evidence, research-run, or strategy output as human
  review, verifier pass, accepted knowledge, or promotion authority.
- Optional Skill package:
  `docs/skills/cosheaf-operator/SKILL.md` documents how a Codex-like operator
  should use Cosheaf. It is a runbook, not runtime authority.

## Included Work

- Post-`v0.4.0` to `v0.5.0` kickoff audit, V9 plan, and ADR 0026.
- Optional read-only operator MCP server core and tests.
- Optional controlled draft/review/runtime MCP tool surface and negative
  authority tests.
- Codex operator runbook and framework-side workspace demo docs.
- Workspace-template operator demo documentation.
- Public KB operator policy smoke docs and policy-guard checks.
- Optional documentation-only Cosheaf operator Skill package.
- v0.5.0 release-candidate metadata and status documentation.
- v0.5.0 tag publication, GitHub release, post-tag release smoke, and
  downstream workspace/public-KB pin alignment.

## Boundaries

- CLI remains the human and CI oracle.
- MCP is an optional local adapter. It is not the primary truth path and is
  not required for ordinary workspace use.
- MCP/operator output is not proof, checked evidence, verifier evidence,
  verifier pass, gate pass, human review, accepted status, accepted refutation,
  or promotion authority.
- Controlled writes may create only draft, proposal, review-context, or
  runtime records already allowed by Cosheaf policy.
- MCP does not write accepted KB content, promote artifacts, mark human
  review, mutate verifier results, call hosted providers, expose arbitrary
  shell, or bypass validation, gates, review, or promotion policy.
- Provider real-run behavior remains explicit, consented, policy-scoped, and
  default-off. CI and default tests do not call real hosted providers or
  require API keys.
- Accepted knowledge still requires validation, gates, human review where
  policy requires it, verifier evidence where applicable, source metadata for
  accepted public artifacts, and explicit promotion.
- Skipped verifier, provider, SAT, SMT, Lean, lake, optional-tool, network, or
  operator results are not passes.
- A successful Lean `#check` means only import and symbol resolution. It does
  not prove informal/formal semantic alignment.
- Public KB accepted content still requires source metadata and human review.

## Downstream Guidance

Downstream repositories that need the Operator MCP + Codex Application Layer
surfaces can pin to:

```text
git+https://github.com/CheemsaDoge/tcs-cosheaf.git@v0.5.0
```

The workspace-template active demo pins and public KB CI pin moved to
`@v0.5.0` only after the public tag, GitHub release, and tag-based release
smoke were complete.

Use `tcs-cosheaf-workspace-template` as the user-facing entry point. Mount or
checkout `tcs-kb-public` as readonly public knowledge and keep private work in
the writable private KB overlay. Do not manually merge framework, public KB,
and private workspace repositories into one mixed tree.

## Release-Candidate Verification

The release-candidate PR runs:

```bash
python -m cosheaf.cli version --json
make lint
make typecheck
make test
make validate
make gate
python scripts/ecosystem_smoke.py --matrix --cosheaf "python -m cosheaf.cli" --framework-root . --workspace-template-root ../tcs-cosheaf-workspace-template --public-kb-root ../tcs-kb-public --json
git diff --check
```

The release-candidate PR intentionally does not create a tag, publish a GitHub
release, run post-tag release smoke, update downstream pins, add runtime
behavior, change schemas, write KB artifacts, create human review, promote
artifacts, change accepted-promotion semantics, or widen MCP/provider
authority.

Observed release-candidate results:

- `python -m cosheaf.cli version --json` reports `0.5.0`.
- At release-candidate start, the framework repository had no open PRs and no
  open issues before issue 374 was created; workspace-template and public KB
  also had no open PRs or issues.
- `make lint`, `make typecheck`, `make test`, `make validate`, and
  `make gate` pass.
- `make test` passes with 679 tests.
- `make validate` reports `Validation passed: checked 20 YAML record(s).`
- `make gate` reports `Gate verdict: pass`.
- The no-network ecosystem matrix with `--framework-tag v0.5.0` reports 17
  rows: 14 pass, 0 fail, and 3 skipped. Optional verifier availability,
  framework git-tag release smoke from `@v0.5.0`, and workspace-template
  install demo remain skipped by default. These skipped rows are
  release-readiness accounting signals, not pass results.
- Downstream workspace-template and public KB worktrees remain clean after the
  matrix run; generated outputs stayed under ignored `.cosheaf/` paths.

## Publication Verification

Issue 376 closes out publication after the release-candidate PR merged cleanly.

- Annotated tag `v0.5.0` is published as tag object
  `49ea4c9de153ccc9c41b39ec2fc705b3735c8795`, pointing through main commit
  `25913148da5e512aa888ec76198146825509e071`.
- GitHub release:
  `https://github.com/CheemsaDoge/tcs-cosheaf/releases/tag/v0.5.0`.
- Release smoke from
  `git+https://github.com/CheemsaDoge/tcs-cosheaf.git@v0.5.0` passed and
  installed `tcs-cosheaf==0.5.0`.
- The release smoke covered help, version, validation, gate, index rebuild,
  and context-build checks.
- Workspace-template PR #71 updated active demo, Makefile, provider-preview,
  fake-provider smoke, verifier-evidence, failure-memory, checked-evidence,
  research-run, strategy, and operator docs/scripts to `@v0.5.0`.
- Public KB PR #86 updated public KB CI and release-facing docs to `@v0.5.0`
  without changing artifacts, review records, formalization metadata, schemas,
  or promotion policy.
- The post-publication ecosystem matrix with `--include-network` and
  `--framework-tag v0.5.0` reports 17 rows: 16 pass, 0 fail, and 1 skipped.
  The skipped row is optional verifier availability and is not counted as
  pass.
- Publication closeout does not add runtime behavior, change schemas, write KB
  artifacts, create human review, promote artifacts, change accepted-promotion
  semantics, or widen MCP/provider authority.

## Known Limitations

- No hosted provider call is default-on.
- No real provider call runs in CI or default tests.
- MCP remains optional and local. It is an adapter over whitelisted Cosheaf
  operations, not a new authority layer.
- SAT, SMT, plain Lean, and external Lean reference checking remain minimal
  optional invocation paths.
- External Lean library reference checking does not fetch CSLib/mathlib or
  prove semantic alignment.
- Strategy plans are useful planning guidance, not mathematical truth.
- Checked evidence is useful review context, not accepted mathematical truth.
- Research-run records are useful provenance, not proof or review authority.
- External public KB integration remains through local workspace roots, not a
  hosted registry service.
