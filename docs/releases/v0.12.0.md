# v0.12.0 - Research Memory Learning + Benchmark Suite v1

Status: published. Package metadata and `cosheaf.__version__` record
`0.12.0`. The public tag, GitHub release, post-tag release smoke, and
downstream workspace-template/public-KB pin updates are complete.

This release packages the V17 Research Memory Learning + Benchmark Suite v1
line on top of the published `v0.11.0` External Operator Campaigns baseline.

## Included Surfaces

- Deterministic sidecar memory updates under `.cosheaf/memory/`:
  update-run sidecars and aggregate weights.
- Memory CLI:
  `cosheaf memory update-from-workflow`, `update-from-campaign`, `rebuild`,
  and `explain`.
- Benchmark suite v1:
  `cosheaf benchmark list`, `run --suite <suite>`, and `report`.
- Comparative reports:
  `cosheaf compare workflows`, `campaigns`, and `benchmarks`.
- Static review reports:
  `cosheaf report workflow`, `campaign`, and `benchmark`.
- Documentation for memory updates, benchmark runs, comparisons, static
  reports, and V17 authority boundaries.

## Authority Boundary

Memory weights are deterministic sidecar guidance only. Benchmark runs,
comparisons, and static reports are regression or review context only. They are
not proof, source metadata, human review, verifier pass, gate pass, accepted
status, accepted theorem/refutation, or promotion authority.

Skipped, unsupported, unavailable, and inconclusive rows are not passes.

This release does not add model training, automatic correctness improvement,
automatic theorem proving, Lean autoformalization, CSLib/mathlib proof
checking, informal/formal semantic alignment, automatic accepted promotion,
accepted writes, human-review creation, default hosted provider calls,
arbitrary shell execution, web UI, production autonomy, or multi-user
permissions.

## Verification

The release-candidate PR ran:

```bash
make lint
make typecheck
make test
make validate
make gate
python -m cosheaf.cli benchmark run --suite regression --json
git diff --check
```

Observed release-candidate results:

- `python -m cosheaf.cli version --json` reports `0.12.0`.
- `make test` reports `847 passed`.
- `make validate` passes with 20 YAML records checked.
- `make gate` reports `Gate verdict: pass`.
- `python -m cosheaf.cli benchmark run --suite regression --json` reports
  `passed: true`, `pass_count: 6`, `fail_count: 0`, `skipped_count: 3`,
  `authority_violation_count: 0`, `private_leak_count: 0`, and
  `skipped_rows_are_passes: false`.
- `git diff --check` passes.

Publication closeout evidence:

- GitHub release:
  <https://github.com/CheemsaDoge/tcs-cosheaf/releases/tag/v0.12.0>.
- Annotated tag object:
  `be58f818f3f8dd75803f514b74acd7485ade2f69`.
- Tagged main commit:
  `8c5e5651acf290883488785b553f623742fca1f6`.
- Post-tag release smoke from
  `git+https://github.com/CheemsaDoge/tcs-cosheaf.git@v0.12.0` passed and
  installed `tcs-cosheaf==0.12.0`.
- Workspace-template active pins moved to `@v0.12.0` in PR #93.
- Public KB CI/docs pins moved to `@v0.12.0` in PR #107.
- The no-network ecosystem matrix with `--framework-tag v0.12.0` reports
  28 rows: 25 pass, 0 fail, and 3 skipped. Skipped rows are not counted as
  pass.

Publication closeout does not add hosted provider defaults, model training, or
accepted promotion semantics.
